import Counter from "../../../components/counter/counter.js";
import { mount } from "enzyme";
import sinon from "sinon";

describe("Counter component Tests", () => {
  it("Will increment the counter when items are added", () => {});
  it("Will increment the counter when items are added", () => {
    expect();
  });
});
